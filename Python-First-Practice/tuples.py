# Tuplas 

my_tuple = (45, 'Perro', 7.4, True)
print(type(my_tuple))

print (my_tuple[1]) # Devuelve al objeto ubicado en la posición especificada
print (my_tuple.count(45)) # Cuenta cuántas veces está el elemento especificado
print (my_tuple.index(True)) # Busca la posición del elemento especificado

# La diferencia entre listas y tuplas es que las listas se pueden modificar y las tuplas no